class Node {
    constructor(valor) {
        this.value = valor;
        this.next = null;
        this.setNext.bind();
    }

    setNext(newNode) {
        if (newNode == null) return;
        this.next = newNode;
    }
}
